---
interface Props {
    title?: string;
}

import Row from "../rows/Row.astro";
import SectionTitle from "../text/SectionTitle.astro";

const { title } = Astro.props;
---

<style>
    .cols-right {
        padding-left: var(--spacingMd);
        padding-right: var(--spacingMd);
    }

    .col1 {
        grid-column: 1 / 5;
    }

    .col2 {
        grid-column: 6 / -1;
    }

    .cols-right-stick {
        top: var(--sizeNavbarY);
        position: sticky;
    }

    .cols-right-title {
        grid-column: 5 / 6;
    }

    @media only screen and (max-width: 600px) {
        .cols-right {
            padding-top: var(--spacingMd);
            padding-bottom: var(--spacingMd);
        }

        .col1 {
            position: sticky;
            grid-column: 1 / -1;
        }

        .col2 {
            grid-column: 1 / -1;
        }

        .cols-right-title {
            grid-column: 1 / -1;
            transform: none;

            padding-left: var(--spacingMd);
            padding-right: var(--spacingMd);
        }
    }
</style>

<Row>
    {
        title ? (
            <SectionTitle class="cols-right-title">{title}</SectionTitle>
        ) : null
    }

    <section class="cols-right col1">
        <slot name="col1" />
    </section>

    <section class="cols-right col2">
        <div class="cols-right-stick">
            <slot name="col2" />
        </div>
    </section>
</Row>
